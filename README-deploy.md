Smart City Darjeeling — Flask + MySQL

Run locally:
1. Create virtualenv, install requirements
2. Initialize DB using db_init.sql
3. Set env vars (DB_* in .env)
4. python backend/app.py
Open http://localhost:5000/
